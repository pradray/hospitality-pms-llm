# Core dependencies — pin top-level packages only, let pip resolve transitive deps

# Vector store
chromadb==1.5.9

# Embeddings & ML
sentence-transformers==5.6.0
torch==2.12.1

# Chat UI
chainlit==2.11.1

# PDF parsing
pymupdf>=1.25.0

# Data processing
pandas>=2.0.0

# Local inference (default backend)
# On Apple Silicon, install with Metal:
#   CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
# On Linux/CUDA:
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python
llama-cpp-python>=0.3.0

# API-based inference (ceiling baselines, LLM-as-judge)
openai>=1.0.0
anthropic>=0.40.0
